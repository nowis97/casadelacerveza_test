interface Theme {
    primaryColor: string;
    secondaryColor:string;
    hoverColor: string;
    borderRadius: number;
}

export const theme: Theme= {
    borderRadius: 8,
    primaryColor: '#f7b62c',
    secondaryColor: '#000',
    hoverColor: '#000'
}